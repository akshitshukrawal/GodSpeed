summary: Update Subscription
tasks:
  - id: schema_subscription_update
    fn: datasource.schema.Subscription.update
    args:
      where:
        id: <% inputs.params.id %>
      data: <% inputs.body %>
