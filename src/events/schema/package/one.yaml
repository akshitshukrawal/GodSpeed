# ONE
http.get./schema/package/{id}:
  summary: Fetch Package
  description: Fetch Package from database
  fn: com.biz.schema.package.one
  params:
    - name: id
      in: path
      required: true
      schema:
        type: string
  responses:
    '200':
      content:
        application/json:
          schema:
            type: object

