# ONE
http.get./schema/subscription/{id}:
  summary: Fetch Subscription
  description: Fetch Subscription from database
  fn: com.biz.schema.subscription.one
  params:
    - name: id
      in: path
      required: true
      schema:
        type: string
  responses:
    '200':
      content:
        application/json:
          schema:
            type: object

