# SEARCH
http.post./schema/subscription/search:
  summary: Fetch multiple Subscription
  description: Fetch multiple Subscription from database
  fn: com.biz.schema.subscription.search
  body:
    content:
      application/json:
        schema:
          type: object
  responses:
    '200':
      content:
        application/json:
          schema:
            type: array

