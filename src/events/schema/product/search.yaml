# SEARCH
http.post./schema/product/search:
  summary: Fetch multiple Product
  description: Fetch multiple Product from database
  fn: com.biz.schema.product.search
  body:
    content:
      application/json:
        schema:
          type: object
  responses:
    '200':
      content:
        application/json:
          schema:
            type: array

