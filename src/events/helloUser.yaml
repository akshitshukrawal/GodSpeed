http.get./verify/user:
  fn: helloUser
  authn: false
  summary: Returns a greeting
  responses:
    200:
      description: Successful greeting
      content:
        application/json:
          schema:
            type: object
            properties:
              message:
                type: string
